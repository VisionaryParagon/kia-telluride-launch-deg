import { NoBreakSpacePipe } from './no-break-space.pipe';

describe('NoBreakSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new NoBreakSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
